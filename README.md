# Temporal Opponent Modeling
This is a repo for the Artificial Intelligence course at TU Delft, for the assignment "Temporal Opponent Modeling".

## Usage
- Clone repo
- Install Python 3
- Run `pip install -r requirements.txt`
- Run `python3 hmm.py --train` (optional)
- Run `python3 hmm.py --test=test2.json`